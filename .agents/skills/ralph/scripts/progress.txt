# Ralph Progress Log
Started: 2026-03-17

## Codebase Patterns
- T3 stack app: Next.js 16 + tRPC 11 + Drizzle ORM + next-auth v5 beta
- App Router with src/app/ directory structure
- ESLint 10 flat config in eslint.config.js using tseslint.config() with @next/eslint-plugin-next directly
- Tailwind CSS 4 with CSS-first config (@import "tailwindcss" in globals.css)
- PostCSS config uses @tailwindcss/postcss plugin
- tRPC uses httpBatchStreamLink with superjson transformer
- Auth uses NextAuth() v5 pattern with Discord provider and DrizzleAdapter
- Env validation via @t3-oss/env-nextjs in src/env.js
- Path alias: ~ → src/
- next.config.js is minimal (empty config object with env import)
- ZodError.flatten() used in tRPC error formatter (src/server/api/trpc.ts)
- Zod 4 is backward compatible: `import { z } from "zod"` and `import { ZodError } from "zod"` both work unchanged
- `next lint` removed in Next.js 16 — use `eslint .` directly in lint script
- `eslint-config-next` v16 exports flat config arrays (can be spread into `tseslint.config()`)
- `eslint-plugin-react` (bundled by eslint-config-next) is NOT compatible with ESLint 10 — use `@next/eslint-plugin-next` configs directly instead
- `eslint-plugin-react-hooks` v7 adds `set-state-in-effect` rule — fix code to avoid synchronous setState in effects

---

## 2026-03-17 - US-001
- Updated all dependency version specifiers in package.json to latest versions
- Removed tailwindcss-animate (redundant with tw-animate-css)
- Updated packageManager to pnpm@10.32.1
- pnpm install completed successfully with expected peer dependency warnings
- Files changed: package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - @tailwindcss/vite 4.2.1 peer dep only supports vite ^5.2.0 || ^6 || ^7, not vite 8 — peer warning expected
  - eslint-plugin-import, eslint-plugin-react-hooks, eslint-plugin-jsx-a11y, eslint-plugin-react don't yet support ESLint 10 — peer warnings expected
  - eslint-config-next 16.1.7 pulls in these plugins with ESLint 9 peer deps
  - sharp@0.34.5 build script is ignored by pnpm — may need `pnpm approve-builds` if sharp is needed
---

## 2026-03-17 - US-002
- Verified Zod 4 migration — no code changes needed
- All existing imports (`ZodError` from `"zod"`, `z` from `"zod"`) work unchanged
- `ZodError.flatten()`, `z.string()`, `z.object()`, `z.enum()`, `.url()`, `.optional()`, `.min()` all verified working
- `tsc --noEmit` passes with no Zod-related errors
- Files changed: prd.json (marked passes: true), progress.txt
- **Learnings for future iterations:**
  - Zod 4 is fully backward compatible with Zod 3 APIs used in this codebase
  - `ZodError` still exports from `"zod"` (not `"zod/v4/core"` as initially feared)
  - @t3-oss/env-nextjs v0.13.x works with Zod 4 without changes
---

## 2026-03-17 - US-003
- Updated lint scripts from `next lint` to `eslint .` (next lint removed in Next.js 16)
- Updated `check` script from `next lint && tsc --noEmit` to `eslint . && tsc --noEmit`
- Updated `lint:fix` script from `next lint --fix` to `eslint --fix .`
- next.config.js required no changes (minimal config is forward-compatible)
- All App Router routes (pages, API routes) work unchanged with Next 16
- `pnpm build` (with SKIP_ENV_VALIDATION=1) passes successfully
- `pnpm typecheck` passes
- Files changed: package.json
- **Learnings for future iterations:**
  - Next.js 16 build requires env vars or SKIP_ENV_VALIDATION=1 due to next.config.js importing src/env.js
  - Next.js 16 auto-updates tsconfig.json include to add `.next/dev/types/**/*.ts`
  - App Router patterns (route handlers, layouts, pages) are fully compatible between Next 15 and 16
  - The `next lint` command is removed in Next.js 16 — must use `eslint .` directly
---

## 2026-03-17 - US-004
- Replaced `eslint-config-next/core-web-vitals` with `@next/eslint-plugin-next` configs directly (recommended + core-web-vitals)
- Removed FlatCompat from `@eslint/eslintrc` — no longer needed
- Added `eslint-plugin-react-hooks` directly (instead of via eslint-config-next)
- Fixed 3 `react-hooks/set-state-in-effect` errors:
  - `src/app/game/page.tsx`: Removed game-over and level-completion useEffects, moved logic into setState callbacks where state changes
  - `src/app/high-scores/page.tsx`: Replaced useEffect + setState with lazy useState initializer for localStorage read
- All custom rules preserved (array-type, consistent-type-definitions, consistent-type-imports, no-unused-vars, require-await, no-misused-promises, drizzle rules)
- `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass
- Files changed: eslint.config.js, src/app/game/page.tsx, src/app/high-scores/page.tsx
- **Learnings for future iterations:**
  - `eslint-plugin-react` (used by eslint-config-next) uses `context.getFilename()` which was removed in ESLint 10 — causes runtime TypeError
  - Bypass `eslint-config-next` entirely and use `@next/eslint-plugin-next` configs directly for ESLint 10 compatibility
  - `eslint-plugin-react-hooks` v7 adds `set-state-in-effect` rule — synchronous setState in useEffect bodies is an error
  - Fix pattern: move state derivation into setState callbacks or use lazy useState initializers for external system reads
  - `@eslint/eslintrc` (FlatCompat) is no longer needed when using native flat configs
---

## 2026-03-17 - US-005
- Removed `@eslint/eslintrc` from devDependencies (FlatCompat no longer used after US-004)
- Removed `eslint-config-next` from devDependencies (replaced by `@next/eslint-plugin-next` direct usage in US-004)
- `tailwindcss-animate` was already removed in US-001, no references found in source
- `pnpm install` completes successfully (167 packages removed)
- All verification commands pass: `pnpm typecheck`, `pnpm lint`, `pnpm build`
- Files changed: package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - After migrating away from `eslint-config-next`, both `eslint-config-next` and `@eslint/eslintrc` can be safely removed
  - Removing `eslint-config-next` also removes its transitive deps (eslint-plugin-react, eslint-plugin-import, etc.) which had ESLint 10 peer dep warnings
  - Final dep count is significantly smaller after cleanup
---
