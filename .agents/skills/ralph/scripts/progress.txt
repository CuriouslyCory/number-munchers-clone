# Ralph Progress Log
Started: 2026-03-17

## Codebase Patterns
- T3 stack app: Next.js 15 + tRPC 11 + Drizzle ORM + next-auth v5 beta
- App Router with src/app/ directory structure
- ESLint 9 flat config in eslint.config.js using FlatCompat + tseslint.config()
- Tailwind CSS 4 with CSS-first config (@import "tailwindcss" in globals.css)
- PostCSS config uses @tailwindcss/postcss plugin
- tRPC uses httpBatchStreamLink with superjson transformer
- Auth uses NextAuth() v5 pattern with Discord provider and DrizzleAdapter
- Env validation via @t3-oss/env-nextjs in src/env.js
- Path alias: ~ → src/
- next.config.js is minimal (empty config object with env import)
- ZodError.flatten() used in tRPC error formatter (src/server/api/trpc.ts)
- Zod 4 is backward compatible: `import { z } from "zod"` and `import { ZodError } from "zod"` both work unchanged
- `next lint` removed in Next.js 16 — use `eslint .` directly in lint script
- `eslint-config-next` v16 exports flat config arrays (can be spread into `tseslint.config()`)

---

## 2026-03-17 - US-001
- Updated all dependency version specifiers in package.json to latest versions
- Removed tailwindcss-animate (redundant with tw-animate-css)
- Updated packageManager to pnpm@10.32.1
- pnpm install completed successfully with expected peer dependency warnings
- Files changed: package.json, pnpm-lock.yaml
- **Learnings for future iterations:**
  - @tailwindcss/vite 4.2.1 peer dep only supports vite ^5.2.0 || ^6 || ^7, not vite 8 — peer warning expected
  - eslint-plugin-import, eslint-plugin-react-hooks, eslint-plugin-jsx-a11y, eslint-plugin-react don't yet support ESLint 10 — peer warnings expected
  - eslint-config-next 16.1.7 pulls in these plugins with ESLint 9 peer deps
  - sharp@0.34.5 build script is ignored by pnpm — may need `pnpm approve-builds` if sharp is needed
---

## 2026-03-17 - US-002
- Verified Zod 4 migration — no code changes needed
- All existing imports (`ZodError` from `"zod"`, `z` from `"zod"`) work unchanged
- `ZodError.flatten()`, `z.string()`, `z.object()`, `z.enum()`, `.url()`, `.optional()`, `.min()` all verified working
- `tsc --noEmit` passes with no Zod-related errors
- Files changed: prd.json (marked passes: true), progress.txt
- **Learnings for future iterations:**
  - Zod 4 is fully backward compatible with Zod 3 APIs used in this codebase
  - `ZodError` still exports from `"zod"` (not `"zod/v4/core"` as initially feared)
  - @t3-oss/env-nextjs v0.13.x works with Zod 4 without changes
---
